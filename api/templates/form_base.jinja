<div class="modal" tabindex="-1" data-editing role="dialog" aria-labelledby="editModalLabel"
    aria-hidden="true" data-backdrop="static" data-focus="true">
    <div class="modal-dialog" role="document">

        {% block modalcontent %}{% endblock modalcontent %}

        {# <div class="modal-footer">
            {{ validation_summary(form) }}
            <button id="btnUpdate" type="button" class="btn btn-primary"
                    onclick="submitModal(this,'{{ submit_target }}');">Save</button>
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
        </div> #}
    </div>
    <script type="text/javascript" charset="utf-8">
      setupModal();
      showDialog();
    </script>
</div>
